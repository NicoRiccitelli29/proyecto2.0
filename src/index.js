import React from 'react';
import ReactDOM from 'react-dom';
import App from './App';


ReactDOM.render( <App /> , document.getElementById('root'));//Renderizo la vista (Lo mando a la vista)
//Este render toma dos parametros, el primero es que vamos a enviar y el segundo es a donde lo envio. 
